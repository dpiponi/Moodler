I can build this on one laptop. I can't build it on any other machine due to cabal conflicts.

This is not the entire thing. Just a minimal set of source files to run one particular test, scripts/test.hs
